export const GenreCard = ({data}) => {
  const { image, title, color } = data;
  // image - URL изображения карточки
  // title - заголовок карточки
  // color - цвет карточки
  return (
    <>
    {title}
    </>
  )
}